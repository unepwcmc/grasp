<% if can? :read, Report %>
  <%= link_to 'Reports', reports_path, class: 'button button-primary' %>
<% end %>
<% if can? :manage, User %>
  <%= link_to 'Users', admin_users_path, class: 'button button-primary' %>
<% end %>
<% if can? :read, Agency %>
  <%= link_to 'Agencies', agencies_path, class: 'button button-primary' %>
<% end %>

<%= link_to 'My Profile', edit_user_registration_path, class: 'button button-primary' %>
