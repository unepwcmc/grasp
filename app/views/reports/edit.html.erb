<%= content_for :page_title, "Edit Report" %>
<%= content_for :breadcrumbs do %>
  <%= breadcrumb "Reports Dashboard", reports_path %>
  <%= breadcrumb "Report ##{@report.id}", report_summary_path(@report.id) %>
  <%= breadcrumb "Edit Report", edit_report_path(@report.id) %>
<% end %>

<div id="report-container" data-report-mode="edit" data-report-id=<%= @report.id %>></div>

<script>var questionnaireTemplate = <%= raw(@questionnaire_template) %></script>
<script>var reportData = <%= raw(@report.data.to_json) %></script>

